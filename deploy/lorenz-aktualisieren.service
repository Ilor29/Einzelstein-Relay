# Eine Aktualisierung von Lorenz' App-Kopie — angestoßen vom Zeitgeber
# (lorenz-aktualisieren.timer), nicht dauerhaft laufend.
#
# WICHTIG (Sicherheit): Dieser Dienst läuft als root. Das ausgeführte Skript
# darf darum NICHT unter /home/roli liegen — dort könnte das roli-Konto es
# umschreiben und sich über den root-Lauf zum Administrator machen. Das Skript
# wird deshalb nach /usr/local/sbin/ installiert (Eigentümer root:root, 755).
#
# Installation:
#   sudo install -o root -g root -m 755 deploy/lorenz-aktualisieren.sh \
#        /usr/local/sbin/hz-lorenz-aktualisieren.sh
#   sudo cp deploy/lorenz-aktualisieren.service deploy/lorenz-aktualisieren.timer \
#        /etc/systemd/system/
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now lorenz-aktualisieren.timer

[Unit]
Description=Lorenz' Hetzner-App auf Rolis Stand bringen

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/hz-lorenz-aktualisieren.sh
